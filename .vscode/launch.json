{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Azure Web App",
      "type": "node",
      "request": "launch",
      "program": "${workspaceFolder}/server.js",
      "cwd": "${workspaceFolder}",
      "env": {
        "PORT": "3000"
      }
    }
  ]
}
